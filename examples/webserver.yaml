devices:
  server:
    type: avr
    mcu: atmega328p
    firmware: examples/http_hello/build/http_hello.elf
    peers:
      - tcp_gateway
#  tcp:
#    type: tcp
#    port: 8000
#    channels:
#      - tcp
